<p align="center">
  <img src="assets/brand/monogram.svg" width="112" alt="Web-Omni vNext">
</p>

<h1 align="center">Web-Omni vNext</h1>

<p align="center">
  面向 Chromium 浏览器的网页工具箱：用一个 Command Hub 管理提取、隐私、阅读、效率、密码、录屏与局域网传输。
</p>

<p align="center">
  <img alt="Version 4.2.0" src="https://img.shields.io/badge/version-4.2.0-1688ff">
  <img alt="Chromium 114+" src="https://img.shields.io/badge/Chromium-114%2B-41b6ff">
  <img alt="Manifest V3" src="https://img.shields.io/badge/Manifest-V3-77df99">
  <img alt="63 commands" src="https://img.shields.io/badge/commands-63-7bd88f">
</p>

Web-Omni vNext 把常用网页操作集中到网页内 Command Hub 和浏览器侧边面板。当前版本注册了 **63 个工具命令**，其中 **62 个可用**、**24 个支持持续状态管理**；翻译、天气、Command Hub 和活动状态系统属于额外的界面能力。

> 当前版本：`4.2.0`  
> 浏览器要求：Chromium `114+`，面向 Chrome 与 Edge  
> 扩展规范：Manifest V3（扩展清单第三版）

## 主要特性

| 能力 | 说明 |
| --- | --- |
| Command Hub | 搜索、分类、收藏和执行命令，支持可打断的视图切换动画 |
| 侧边面板 | 查看当前页面、收藏快捷操作、活动功能、翻译和天气 |
| Active Actions | 集中显示持续运行的功能，并提供关闭、撤销、恢复和管理入口 |
| 密码库 | 本地加密凭据、快捷填充、保存确认、密码生成和加密备份 |
| 局域网传输 | WebRTC P2P（点对点）文件、图片、文本和链接传输 |
| 页面工具 | 元素消除、数据提取、隐私检查、阅读模式、标注、监控和录屏 |

## 功能清单

| 分类 | 数量 | 工具 |
| --- | ---: | --- |
| 视觉掌控 | 2 | 元素消除、规则管理 |
| 数据提取 | 11 | 提取图片/视频、电商图片爬取、剪藏 Markdown、框选提取、链接批量提取、结构化数据、CSS 选择器爬取、邮箱电话嗅探、页面快照、页面源码、API 端点嗅探 |
| 电商 | 1 | 多平台比价 |
| 密码管理 | 4 | 密码金库、密码生成器、一键填充、自动保存 |
| 隐私保护 | 8 | 隐私评分、追踪器拦截、指纹保护、WebRTC 防护、清除 Referrer、清除 Cookie、一键清痕、防截图模式 |
| 安全工具 | 11 | Cookie 提取、隐藏字段/Token、Storage 转储、密码明文显示、全局变量转储、事件监听、网络请求监听、浏览器指纹、WebSocket 监听、JS 代码注入、Canvas 伪装 |
| YouTube | 7 | 快捷工具面板、跳过广告、A-B 循环播放、影院模式、视频截图、视频信息、音频流嗅探 |
| 自动化 | 1 | 闪电填表 |
| 沉浸阅读 | 3 | 暗黑模式、阅读器模式、解除复制限制 |
| 实用工具 | 4 | 屏幕录制、页面二维码、性能速查、页面标注 |
| 效率工具 | 10 | 膏药清理、链接净化、全页链接净化、输入框保护、输入框历史、音频均衡、均衡器面板、元素画中画、添加监控、监控仪表盘 |
| 文件传输 | 1 | 局域网传输 |

`JS 代码注入` 受 Manifest V3 的 Content Security Policy（内容安全策略）限制，当前保留在命令表中并显示禁用原因。

## 安装

### 使用发行包

1. 下载 [Web-Omni-vNext-4.2.0.zip](release/Web-Omni-vNext-4.2.0.zip)。
2. 将 ZIP 解压到固定目录。
3. 在 Chrome 打开 `chrome://extensions/`；在 Edge 打开 `edge://extensions/`。
4. 开启“开发者模式”或“开发人员模式”。
5. 点击“加载已解压的扩展程序”或“加载解压缩的扩展”。
6. 选择包含 `manifest.json` 的解压目录。
7. 在扩展菜单中固定 Web-Omni 图标，并刷新已打开的网页。

### 使用源代码

下载仓库源码并解压，然后在扩展管理页选择项目根目录。根目录应同时包含 `manifest.json`、`background.js`、`content-scripts/` 和 `side-panel/`。

### 更新版本

替换本地文件后，在扩展管理页点击 Web-Omni 卡片上的“重新加载”，随后刷新目标网页。重新加载扩展会结束当前标签页中的临时活动状态。

## 快速开始

1. 打开普通的 `http://` 或 `https://` 网页。
2. 点击工具栏中的 Web-Omni 图标，进入侧边面板。
3. 使用 `Ctrl+Shift+K` 打开 Command Hub，macOS 使用 `Command+Shift+K`。
4. 输入关键词搜索命令，或者进入“全部工具”按分类浏览。
5. 点击星标收藏常用命令；侧边面板会同步收藏，并支持拖动排序。
6. 持续运行的工具会出现在“当前活动”和网页侧边控制按钮中，可随时关闭或恢复。

### 快捷键

| Windows / Linux | macOS | 功能 |
| --- | --- | --- |
| `Ctrl+Shift+K` | `Command+Shift+K` | 打开或关闭 Command Hub |
| `Alt+S` | `Alt+S` | 清理当前页面的悬浮元素 |

浏览器可能占用部分组合键。可在 `chrome://extensions/shortcuts` 或 `edge://extensions/shortcuts` 中重新设置。

## 常用工作流

### 元素消除与恢复

1. 在 Command Hub 中运行“元素消除”。
2. 在页面上选择需要隐藏的元素。
3. 从“当前活动”使用“撤销”恢复最近一次操作。
4. 使用“全部恢复”清除当前页面的隐藏规则，或者进入“规则管理”查看记录。

元素通过隐藏标记处理，原始 DOM（文档对象模型）节点会保留，便于撤销和恢复。

### 密码库

1. 打开“密码金库”，设置至少 12 位主密码和 4–8 位数字安全码。
2. 解锁后添加站点、用户名、密码和备注。
3. 在登录页使用密码库快捷窗口执行一键填充。
4. 开启“自动保存”后，提交登录表单时会出现保存确认。
5. 在完整密码库中导出 v3 加密备份。

密码库闲置 5 分钟后自动锁定。从完整密码库复制凭据后，扩展会在约 30 秒后检查剪贴板，内容仍匹配时尝试清除；浏览器焦点和剪贴板权限可能影响清理结果。

### 局域网传输

1. 运行“局域网传输”，电脑端会生成 6 位房间码和二维码。
2. 下载手机页面，或导出局域网页包并通过本地网页服务打开。
3. 手机端输入房间码连接电脑。
4. 连接后可拖入文件、选择文件、粘贴截图，或发送文本和链接。

传输协议包含分片校验、批量确认、缺片重传、断线重试和发送背压控制。文件与消息通过 WebRTC DataChannel（数据通道）传输；PeerJS 信令服务参与连接协商。

### 翻译与天气

- 侧边面板“翻译”支持选中文本、短句和整页翻译，整页翻译可恢复原文。
- “天气”支持城市搜索和浏览器定位，并显示当前天气与预报。
- 翻译会把文本片段和语言参数发送到 MyMemory。
- 天气会把城市名或定位坐标发送到 Open-Meteo。

### 屏幕录制

1. 运行“屏幕录制”。
2. 由浏览器选择要共享的标签页、窗口或屏幕。
3. 根据需要启用系统音频或麦克风。
4. 开始录制并在完成后保存文件。

支持 File System Access API（文件系统访问接口）时优先流式写入；内存降级模式达到 `512 MiB` 后会自动停止并保存。

## 数据与安全

### 密码库保护

- 凭据使用 AES-256-GCM 加密后保存在 `chrome.storage.local`。
- 密钥通过 PBKDF2-SHA-256、随机盐和 90 万次迭代派生。
- 主密码与安全码参与密钥派生；持久存储内容限于派生参数与密文。
- 解锁密钥仅保留在密码库页面内存中，锁定后清理运行状态。
- 旧版 60 万次迭代数据在成功解锁后换盐迁移。
- 导入限制为 5 MiB、最多 5000 条，并校验备份和密文结构。

浏览器密码库的安全性还取决于设备安全、浏览器配置和其他扩展。重要凭据建议保留独立的加密备份。

### 外部通信

| 功能 | 外部服务 | 发送内容 |
| --- | --- | --- |
| 翻译 | MyMemory | 待翻译文本片段、源语言和目标语言 |
| 天气 | Open-Meteo | 城市搜索词，或用户触发定位后得到的经纬度 |
| 局域网传输 | PeerJS 信令、Google/Cloudflare STUN | Peer ID、SDP/ICE 连接协商信息；文件内容走 WebRTC 数据通道 |
| 媒体下载 | 页面资源源站 | 用户选择下载的资源请求 |

项目自身当前未集成独立遥测、广告或用户账户系统。使用翻译、天气、局域网传输和远程媒体功能时需要网络连接，并受相应服务可用性与隐私政策约束。

## 权限说明

| 权限 | 用途 |
| --- | --- |
| `storage` | 保存设置、收藏、规则、历史记录、监控配置和加密密码库 |
| `activeTab` | 在用户操作后访问当前标签页 |
| `scripting` | 按需注入工具脚本与样式 |
| `tabs` | 获取活动标签页、同步状态以及创建内部工具页 |
| `sidePanel` | 提供浏览器侧边面板入口 |
| `contextMenus` | 提供右键菜单命令 |
| `downloads` | 保存提取结果、媒体和生成文件 |
| `alarms` | 定期检查持久 DOM 监控任务 |
| `notifications` | Manifest 已声明；当前版本尚无常驻通知流程 |
| `<all_urls>` | 让页面工具可在用户访问的网站上按需运行，并支持媒体资源读取 |

## 兼容性与限制

- 最低浏览器版本为 Chromium `114`。
- Chrome 内部页、Edge 内部页、扩展页、DevTools、`data:`、`file:` 和 `view-source:` 页面属于受限环境。
- YouTube 专用工具仅在 YouTube 页面启用。
- 页面标注和元素画中画需要有效的用户手势。
- 防截图模式只能遮挡网页内容，操作系统截图、外部拍摄和浏览器之外的录制仍可获取画面。
- Cookie 工具只能读取页面可访问的 Cookie；`HttpOnly` Cookie 由浏览器隔离，页面脚本无法读取。
- 追踪器、请求与 WebSocket 监听从启用时开始观察，已经发生的请求或已经加载的资源无法回溯。
- 局域网传输依赖 WebRTC、信令服务和当前网络策略；严格 NAT、防火墙或企业网络可能阻止连接。

## 常见问题

### Command Hub 无法打开

确认当前页面属于普通网站，随后在扩展管理页重新加载 Web-Omni，并刷新网页。浏览器内置页面和扩展商店页面禁止内容脚本注入。

### 密码库快捷窗口显示锁定

点击“更多”打开完整密码库，输入主密码和安全码完成解锁，再回到来源网页执行填充或保存。

### 局域网设备连接失败

优先让两台设备连接同一网络，确认 WebRTC 和 PeerJS 信令服务可访问，并重新生成房间码。企业代理、防火墙和严格 NAT 环境可能需要调整网络策略。

### 更新后网页仍显示旧界面

在扩展管理页点击“重新加载”，再刷新所有已打开的目标网页。旧标签页会继续保留旧内容脚本，刷新后使用新版本。

## 项目结构

```text
Web-Omni-vNext/
├─ manifest.json          # Manifest V3 配置
├─ background.js          # 后台动作路由与状态管理
├─ content-scripts/       # Command Hub 与页面功能模块
├─ side-panel/            # 侧边面板
├─ shared/                # 动作注册表与共享运行时
├─ styles/                # Command Hub 共用样式
├─ vault/                 # 密码库完整页与快捷窗口
├─ lan-transfer/          # WebRTC 局域网传输
├─ screen-recorder/       # 屏幕录制器
├─ assets/                # 图标与品牌资源
├─ tests/                 # 契约检查
└─ release/               # 可分发发行包
```

## 本地检查

项目当前使用 Node.js 内置断言执行动作注册表检查，可在空白依赖环境运行。

```bash
node --check background.js
node --check shared/action-registry.js
node --check content-scripts/command-hub.js
node tests/action-registry.test.cjs
```

预期契约结果：`63 commands, 24 persistent actions`。

## 发行文件

- ZIP：[release/Web-Omni-vNext-4.2.0.zip](release/Web-Omni-vNext-4.2.0.zip)
- SHA-256：`637186728416665D107A13FE28304B4A5ED7C8776525D6F62EB344C6092FAAD9`
- ZIP 根目录直接包含 `manifest.json`，可解压后加载。

## 提交问题

提交 Issue 时请附上：

- Web-Omni 版本与浏览器版本
- 出现问题的页面类型
- 可重复的操作步骤
- 已移除账号、密码、Token 和 Cookie 的控制台错误信息

涉及凭据、Cookie、页面源码或局域网连接信息时，请先删除敏感内容再公开提交。
